name: CI

on:
  push:
    branches: 
    - dev
    - feature/*
    - hotfix/*
  pull_request:
    branches: 
    - master
    - dev
    - release/*

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
    - name: Install dependencies
      run: npm ci
    - name: Run tests
      run: npm test
